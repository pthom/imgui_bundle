# yaml
name: Build and Publish Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'docs/book/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r docs/book/requirements.txt

      - name: Install Typst
        uses: typst-community/setup-typst@v4

      - name: Build Jupyter Book (and PDF)
        run: |
          cd docs/book
          BASE_URL="/imgui_bundle" jupyter-book build --html
          jupyter-book build --pdf
          mkdir -p _build/html/assets
          cp _build/exports/book.pdf _build/html/assets/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/book/_build/html

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: >
      github.event_name == 'push' ||
      github.event_name == 'workflow_dispatch'
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
